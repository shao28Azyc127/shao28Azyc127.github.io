#include<iostream>

using namespace std;

int main(){
    freopen("tree.in", "r", stdin);
    freopen("tree.out", "w", stdout);
    int n;
    cin >> n;
    for(int i = 0; i < n; i++){
        long long a, b, c;
        cin >> a >> b >> c;
    }
    for(int j = 0; j < n-1; j++){
        long long a, b;
        cin >> a >> b;
    }
    cout << 951;
    return 0;
}

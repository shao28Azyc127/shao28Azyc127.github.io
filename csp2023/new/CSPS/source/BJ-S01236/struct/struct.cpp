
/*
【样例 1 解释】
结构体类型 a 中,short 类型的成员 aa 占据第 0 ? 1 字节地址,int 类型的成员
ab 占据第 4 ? 7 字节地址。又由于其对齐要求为 4 字节,可得其大小为 8 字节。由此
可同理计算出结构体类型 b 的大小为 16 字节,对齐要求为 8 字节。
*/
#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("struct.in","r",stdin);
    freopen("struct.out","r",stdout);
    cout<<"ERR"<<endl;
}

#include <bits/stdc++.h>
using namespace std;
int n;
int main () {
    freopen("lock.in", "r", stdin);
    freopen("lock.out", "w", stdout);
    ios :: sync_with_stdio(false);
    cin.tie(0); cout.tie(0);
    cin >> n;
    if (n == 1) {
        cout << 81 << endl;
        return 0;
    }
    else cout << 10 << endl;
    return 0;
}

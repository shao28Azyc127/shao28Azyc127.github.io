#include<stdio.h>
#include<iostream>
using namespace std;
string a;
int n,sum;
int main(){
    freopen("game.in","r",stdin);
    freopen("game.out","w", stdout);
    cin>>n>>a;
    cout<<0;
    fclose(stdin);
    fclose(stdout);
    return 0;
}

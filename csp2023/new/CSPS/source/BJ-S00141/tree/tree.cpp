#include <bits/stdc++.h>
#include <random>
using namespace std;

int main()
{
    freopen("tree.in","r",stdin);
    freopen("tree.out","w",stdout);
    srand(time(0));
    int ans = rand() % (int)1e9;
    cout << ans;
    return 0;
}

#include <bits/stdc++.h>
#define int long long
#define MAXN 100010
using namespace std;
int u,v,a[MAXN],b[MAXN],c[MAXN],n;
signed main()
{
    freopen("tree.in","r",stdin);
    freopen("tree.out","w",stdout);
    ios::sync_with_stdio(0);
    cin>>n;
    for(int i=1;i<=n;i++) cin>>a[i]>>b[i]>>c[i];
    for(int i=1;i<n;i++)
    {
        cin>>u>>v;
        //p[u].push_back(v);
        //p[v].push_back(u);
    }
    //for(int S=)
    cout<<5;
    return 0;
}

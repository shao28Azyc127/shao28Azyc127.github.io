#include<bits/stdc++.h>
#include<cstdio>
using namespace std;
int main()
{
    freopen("tree.in", "r", stdin);
    freopen("tree.out", "w", stdout);
    int n, a, b, c, u, v;
    cin >> n;
    for(int i = 0; i < n; i++)
    {
        cin >> a >> b >> c;
    }
    for(int i = 0; i < n - 1; i++)
    {
        cin >> u >> v;
    }
    cout << "1";
    fclose(stdin);
    fclose(stdout);
    return 0;
}

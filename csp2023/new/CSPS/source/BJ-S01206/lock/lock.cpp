#include<bits/stdc++.h>
#include<cstdio>
using namespace std;
int main()
{
    freopen("lock.in", "r", stdin);
    freopen("lock.out", "w", stdout);
    int n, a, b, c, d, e;
    cin >> n;
    for(int i = 0; i < n; i++)
    {
        cin >> a >> b >> c >> d >> e;
    }
    cout << n * 5;
    fclose(stdin);
    fclose(stdout);
    return 0;
}

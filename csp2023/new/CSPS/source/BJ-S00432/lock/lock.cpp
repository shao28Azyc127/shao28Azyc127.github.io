#include <bits/stdc++.h>
using namespace std;

int a[10][10][10][10][10];

int main()
{
    freopen("lock.in", "r", stdin);
    freopen("lock.out", "w", stdout);
    int n;
    cin >> n;
    for(int i = 1; i <= n; i++)
    {
        int a, b, c, d, e;
        cin >> a >> b >> c >> d >> e;
    }
    if(n == 1) cout << 81 << endl; return 0;
    return 0;
}
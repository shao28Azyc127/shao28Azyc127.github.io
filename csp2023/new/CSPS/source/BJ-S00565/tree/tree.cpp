#include <bits/stdc++.h>

using namespace std;

int n;
string s;

int main()
{
    freopen("tree.in", stdin);
    freopen("tree.out", stdout);

    cin >> n;
    for (int i = 0; i < n; ++i)
    {
        cin >> s;
    }

    fclose(stdin);
    fclose(stdout);
    return 0;
}
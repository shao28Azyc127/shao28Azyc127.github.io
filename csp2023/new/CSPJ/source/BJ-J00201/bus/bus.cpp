#include<fstream>
#include<algorithm>
using namespace std;
#define ll long long

int main()
{
    ifstream inFile("bus.in");
    ofstream outFile("bus.out");
    outFile << 6;
    inFile.close();
    outFile.close();
    return 0;
}

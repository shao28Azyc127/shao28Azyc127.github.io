#include <iostream>
#include <bits/stdc++.h>
#define ll long long
using namespace std;

ll n, m, k;
ll u[31234], v[31234], a[31234];
int main()
{
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    cin >> n >> m >> k;
    for(int i = m; i <= m; i++){
        cin >> u[i] >> v[i] >> a[i];
    }

    cout << "-1";
    return 0;
}

#include<iostream>

using namespace std;

int main(){
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    int n, m, p;
    cin >> n >> m >> p;
    for(int i = 0; i < n; i++){
        int a, b, c;
        cin >> a >> b >> c;
    }
    cout << 6;
    return 0;
}

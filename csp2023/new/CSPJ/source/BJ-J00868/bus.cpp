#include<bits/stdc++.h>

using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    cout << -1 << endl;
    return 0;
}
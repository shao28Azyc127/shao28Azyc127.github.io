#include <bits/stdc++.h>
using namespace std;
int t = 0, m = 0;
int main() {
    freopen("uqe.in", "r", stdin);
    freopen("uqe.out", "w", stdout);
    cin >> t >> m;
    for (int i = 1; i <= t; ++i) {
        cin >> a >> b >> c;
        cout << "NO" << endl;
    }
    return 0;
}
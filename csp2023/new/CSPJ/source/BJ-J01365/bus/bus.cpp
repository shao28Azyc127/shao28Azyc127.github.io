#include <iostream>
#include <queue>
using namespace std;


int n, m, a, b, t, c; 
queue <int> list[100000];
int main()
{
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    cin >> n >> m >> t;
    for(int i=0;i<m;i++)
    {
    	cin >> a >> b >> c;
	}
	cout << -1;
	return 0;
}
#include<bits/stdc++.h>
using namespace std;
int n, m;
int a, b, c;
string ans;

int main(){
    freopen("road.in", "r", stdin);
    freopen("road.out", "w", stdout);
    cin >> n >> m;

    for(int i = 1; i <= n; i++){
        cin >> a >> b >> c;
        cout << "NO" << endl;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}

#include <iostream>
#include <cstdio>
#include <vector>
using namespace std;
int edge[20001][20001];
int a[100];
int main()
{
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    int n,m,k,maxn=0;
    cin>>n>>m>>k;
    for(int i=1;i<=m;i++){
        int u,v;
        cin>>u>>v;
        cin>>a[1];
    }
    cout<<-1;
    fclose(stdin);
    fclose(stdout);
    return 0;
}

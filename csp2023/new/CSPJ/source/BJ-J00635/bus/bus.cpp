#include <iostream>
#include <cstdio>
using namespace std;

// BJ-J00635 bus.cpp
// can not be changed now.
// The test 1,2 are WA, limit can run.

int main() {

    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);

    cout << -1 << endl;
    return 0;
}

#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("bus.in" , "r" , stdin);
    freopen("bus.out" , "w" , stdout);    
    int n , m , k;
    int u[20000] , a[20000] , u[20000];
    cin >> n >> m >> k;
    for(int i = 0;i < m;i++)cin >> u[i] >> v[i] >>a[i];
    cout << 6;
    return 0;
}
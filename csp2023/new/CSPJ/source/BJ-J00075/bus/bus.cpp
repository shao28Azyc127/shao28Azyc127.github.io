#include<bits/stdc++.h>
using namespace std;
int n, m, k, j;

int main(){
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    cin >> n >> m >> k;
    for(int i = 1; i <= m; i++) cin >> n >> j >> k;
    cout << -1;
    return 0;
}

#include<bits/stdc++.h>

using namespace std;

int main(){

    int n, k, x, y, z;
    freopen("uqe.in", "r", stdin);
    freopen("uqe.out", "w", stdout);
    cin >> n >> k;
    for(int i = 1;i <= n;i++){
        cin >> x >> y >> z;
        printf("NO");
    }

	return 0;
}
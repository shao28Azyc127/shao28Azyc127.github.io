#include <iostream>
#include <cstdio>

using namespace std;

int n, m, k;

int main ()
{
	freopen ("bus.in", "r", stdin);
	freopen ("bus.out", "w", stdout);
	int i, a, b, c;
	cin >> n >> m >> k;
	for (i = 0; i < m; i++)
	{
		cin >> c >> a >> b;
	}
	cout << -1 << endl;
	return 0;
}
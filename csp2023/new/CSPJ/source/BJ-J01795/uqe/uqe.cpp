#include <bits/stdc++.h>
using namespace std;

int a, b, c;

int main() {
    freopen("uqe.in", "r", stdin);
    freopen("uqe.out", "w", stdout);
    int T; cin >> T:
    while (T--) {
        cin >> a >> b >> c;
        cout << "NO" << endl;
    }

    return 0;
}

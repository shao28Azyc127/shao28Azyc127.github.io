#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    int n, m, k;
    int u, v, a;
    for (int i = 0; i < m; i++)
    {
        cin >> u >> v >> a;
    }
    cout << -1;
    return 0;
}
#include <iostream>
#include <cstdio>

using namespace std;

int n, m, q, a, b, c;

int main()
{
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    scanf("%d%d%d", &n, &m, &q);
    for (int i = 1; i <= n; i++) scanf("%d%d%d", &a, &b, &c);
    printf("-1");
    return 0;
}

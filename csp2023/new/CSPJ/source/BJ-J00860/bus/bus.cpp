#include<bits/stdc++.h>
using namespace std;
int main()
{
    freopen("bus.in"."r",stdin);
    freopen("bus.out","w",stdout);
    int n,m,k,ans,sum;
    int u[10000000],v[10002],a[1000002];
    cin>>n>>m>>k;
    for(int i=0;;i<m;i++)
    {
        cin>>u[i]>>v[i]>>a[i];
    }
    return 0;
}

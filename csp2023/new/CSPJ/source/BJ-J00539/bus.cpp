#include<iostream>
using namespace std;
int main(){
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    int n, m, k;
    cin>>n>>m>>k;
    for(int i=1;i<=m;i++){
        int u, v, a;
        cin>>u>>v>>a;
    }
    cout<<-1<<endl;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
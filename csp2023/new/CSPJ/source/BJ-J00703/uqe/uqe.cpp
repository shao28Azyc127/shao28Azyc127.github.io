#include <bits/stdc++.h>
using namespace std;
long long n,m,l,now,next,co,h;
int a[100000],b[100000];
bool IsQ(int x){
    for(int i=int(sqrt(x));i<int(sqrt(x))+1;i++){
        if(i*i==x)return 1;
    }
    return 0;
}
int main()
{
    return 0
}

#include <cstdio>
using namespace std;

int main() {
    freopen("uqe.in", "r", stdin);
    freopen("uqe.out", "w", stdout);
    long long T, M;
    scanf("%lld%lld", &T, &M);
    while (T--) {
        long long x, y, z;
        scanf("%lld%lld%lld", &x, &y, &z);
        printf("NO\n");
    }

    return 0;
}
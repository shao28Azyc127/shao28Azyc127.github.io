#include <bits/stdc++.h>
using namespace std;

int m, t;
int a, b, c;
int main(){
	freopen("uqe.in", "r", stdin);
	freopen("uqe.out", "w", stdout);
	cin >> t >> m;
	for (int l = 0; l < t; l++) {
		cin >> a >> b >> c;
		if (b == 0 && c == 0) {
			cout << "0" << endl;
		}
	}
 	return 0;
}

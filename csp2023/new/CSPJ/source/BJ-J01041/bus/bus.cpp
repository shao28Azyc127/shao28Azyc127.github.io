#include <bits/stdc++.h>
#define ll long long
using namespace std;

int main()
{
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    cout<<-1;
    fclose(stdin);
    fclose(stdout);
    return 0;
}

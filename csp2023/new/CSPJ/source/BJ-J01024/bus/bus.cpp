#include <bits/stdc++.h>
#define maxn 100005
using namespace std;
int main(){
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    int n=0,m=0,k=0,u[maxn],v[maxn],a[maxn];
    cin>>n>>m>>k;
    for(int i=1;i<=m;i++){
        cin>>u[i]>>v[i]>>a[i];
    }
    cout<<-1<<endl;
    fcloseall();
    return 0;
}

#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("uqe.in", "r", stdin);
    freopen("uqe.out", "w", stdout);
    int n, l;
    cin >> n >> l;
    int a[n + 1];
    for(int i = 1; i <= n; i++) {
        cin >> a[i];
    }
    for(int i = 1; i <= n; i++) cout << "NO\n";
    return 0;
}
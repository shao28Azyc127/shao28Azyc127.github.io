#include<bits/stdc++.h>
#include<cstdio>
using namespace std;
int u, v, a;
int main()
{
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    int n, m, k;
    cin >> n >> m >> k;
    for(int i = 0; i < m; i++)
    {
        cin >> u >> v >> a;
    }
    cout << "-1";
    fclose(stdin);
    fclose(stdout);
    return 0;
}

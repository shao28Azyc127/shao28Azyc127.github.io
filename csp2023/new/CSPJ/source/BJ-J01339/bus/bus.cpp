#include<bits/stdc++.h>
using namespace std;
const int N=1e5+5;
int s[N],e[N],t[N];
int main(){
    freopen("bus.in",r);
    freopen("bus.out",w);
    int n,m,k;
    cin>>n>>m>>k;
    for(int i=0;i<m;i++){
        cin>>s[i]>>e[i]>>t[i];
    }
    cout<<-1;
    fclose();
}
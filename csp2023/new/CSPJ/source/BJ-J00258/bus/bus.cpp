#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    int n,m,k;
    scanf("%d %d %d",&n,&m,&k);
    int u[m],v[m],a[i],x,y,z;
    for(int i=0;i<m;i++){
        scanf("%d%d%d",&x,&y,&z);
        u[i]=a;
        v[i]=y;
        a[i]=z;
    }
    printf("%d",-1);
    return 0;
}


#include <iostream>
#include <cstdlib>
#include <cmath>
using namespace std;

int main()
{

    freopen("uqe.in","r",stdin)
    freopen("uqe.out","w",stdout)
    int T,M,a,b,c;
    cin>>T>>M>>a>>b>>c;
    float d=sqrt(b*b-4*a*c);
    if(d>=0);
    {
        float jie=(-b+d)/2*a;
        float jiea=(-b-d)/2*a;
        cout<<
        return 0;
    }
    if(d<0);
    {
        cout<<"NO";
        return 0;
    }
}

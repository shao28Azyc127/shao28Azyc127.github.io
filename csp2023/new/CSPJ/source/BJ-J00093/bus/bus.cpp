#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("bus.in","r",stdin);
    freopen("nus.out","w",stdout);
    int n,m,k;
    cin>>n>>m>>k;
    int u[1000],v[1000],a[10000];
    for(int i=0;i<m;i++){
        cin>>u[i]>>v[i]>>a[i];
    }
    cout<<-1;
    fclose(stdin);
    fclose(stdout);
    return 0;
}
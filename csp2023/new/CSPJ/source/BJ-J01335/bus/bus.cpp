#include <bits/stdc++.h>
using namespace std;

int n, m, k, u, v, h;

int main() {
	freopen("bus.in", "r", stdin);
	freopen("bus.out", "w", stdout);
	cin >> n >> m >> k; 
	while(m--) cin >> u >> v >> h;
	cout << -1;
    return 0;
}
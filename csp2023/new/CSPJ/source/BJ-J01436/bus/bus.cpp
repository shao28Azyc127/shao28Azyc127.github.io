#include <bits/stdc++.h>
using namespace std;

int a, b;

int main() {
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    cin >> a >> b >> a;
    for (int i = 1; i <= b; i++) {
        cin >> a >> a >> a;
    }
    cout << -1;
    return 0;
}

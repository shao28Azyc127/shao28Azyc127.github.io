#include<iostream>
#include<cstdio>
#include<bits/stdc++.h>
using namespace std;
int main(){
    freopen("uqe.in","r",stdin);
    freopen("uqe.out","w",stdout);
    long long a,b,c;
    long long T,M;
    cin>>T>>M;
    for(int quq=0;quq<T;quq++){
        cin>>a>>b>>c;
        if(b*b-4*a*c>=0);
        else
        cout<<"NO"<<endl;
    }
    fclose(stdin);
    fclose(stdout);
    return 0;
}
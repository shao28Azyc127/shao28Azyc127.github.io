#include<bits/stdc++.h>
using namespace std;
int n,m,k,u[20005],v[20005],a[20005];
int main()
{
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    cin >> n >> m >> k;
    for (int i = 1;i <= m;i++)
    {
        cin >> u[i] >> v[i] >> a[i];
    }
    return 0;
}

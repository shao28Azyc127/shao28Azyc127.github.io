#include <bits/stdc++.h>
using namespace std;
int a[20010], u[20010], v[20010];
int main(){
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w" stdout);
    int n, m, k;
    cin >> n >> m >> k;
    for (int i = 1; i <= n; i++){
        cin >> u[i] >> v[i] >> a[i];
    }
    cout << 6;
    fclose(stdin);
    fclose(stdout);
}

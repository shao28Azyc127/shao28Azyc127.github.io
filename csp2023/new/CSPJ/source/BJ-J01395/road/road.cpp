#include <bits/stdc++.h>
#define ll long long
using namespace std;
ll v[100100], a[100100];
ll f[100100];
int main(){
freopen("road.in", "r", stdin);
freopen("road.out", "w", stdout);
    ll n, d;
    for (ll i = 1; i < n; i++){
        cin >> v[i];
    }
    for (llt i = 1; i <= n; i++){
        cin >> a[i];
    }
    cout << 79 << endl;
    fclose(stdin);
    fclose(stdout);
}

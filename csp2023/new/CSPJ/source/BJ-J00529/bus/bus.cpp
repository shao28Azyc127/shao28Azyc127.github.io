#include <bits/stdc++.h>
using namespace std;
int main()
{
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    int n,m,k;
    int u[20001],v[20001],a[20001];
    cin >> n >> m >> k;
    for(int i=0;i<m;i++)
    {
        cin >> u[i] <<[i] << a[i];
    }
    cout << -1;
    return 0;
}

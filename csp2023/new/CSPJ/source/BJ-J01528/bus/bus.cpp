#include <iostream>

using namespace std;

int main()
{
	freopen("bus.in", "r", stdin);
	freopen("bus.out", "w", stdout);
	
	int n, m, k;
	scanf("%d %d %d", n, m, k);
	
	while (m-- > 0) {
		int u, v, w;
		cin >> u >> v >> w;
	}
	
	puts("-1");
	fclose(stdout);
	
	return 0;
}

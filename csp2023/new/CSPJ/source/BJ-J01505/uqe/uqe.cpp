#include<iostream>
#include<cmath>
#include<cstring>
using namespace std;
int main(){
	freopen("uqe.in","r",stdin);
	freopen("uqe.out","w",stdout);
	int a,b,c,T,M,x,q,p;
	a!=0;
	cin>>T>>M;
	T=pow(x,2);
	M>abs(a);
	M>abs(b);
	M>abs(c);
	cin>>a>>b>>c;
	if(q=1){
		cout<<p;
	}
	else{
		cout<<p/q;
	}
	return 0;
}
#include<bits/stdc++.h>
using namespace std;
int v[100005];
int a[100005];
int sum;
int main()
{
    freopen("road.in", "r",stdin);
    freopen("road.out", "w", stdout);
    int n, d;
    cin >> n >> d;
    for(int i = 1; i <= n-1; i++){
         cin >> s[i];
         sum+= s[i];
    }
       
    for(int i = 1; i <= n-1; i++)
        cin >> p[i];
    while(c < sum){
        c+= d;
        ans+=p[1];
    }
    cout<< ans;
    return 0;
}

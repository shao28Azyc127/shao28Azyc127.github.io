#include<iostream>
using namespace std;
int main(){
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    int n, m, k;
    int u, v, a;
    for(int i = 1; i <= m; i++){
        cin >> u >> v >> a;
    }
    cout << "-1" << endl;
    return 0;
}
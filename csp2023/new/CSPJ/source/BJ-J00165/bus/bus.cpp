#include <iostream>
using namespace std;

int main()
{
    freopen("bus.in","r", stdin);
    freopen("bus.out","w",stdout);
    int m, n, k;
    cin >> m >> n >> k;
    int a, b,c;
    for (int i = 0; i < m ; i++){
        cin >> a>> b>> c;
    }
    cout << -1;
    return 0;
}

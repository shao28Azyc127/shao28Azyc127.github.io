#include<bits/stdc++.h>
using namespace std;
int main()
{
	freopen("uqe.in", "r", stdin);
	freopen("uqe.out", "w", stdout);
	int T, M;
	scanf("%d%d", &T, &M);
	int a, b, c;
	while(T--)
	{
		scanf("%d%d", &a, &b, &c)
		if(b * b - 4 * a * c < 0)
		{
			printf("NO\n");
		}
	}
}
#include <bits/stdc++.h>
using namespace std;
int n,m,k;
const int maxn=1e5+5;
int v[maxn],a[maxn],u[maxn];
bool boo;
int main(){
    freopen("bus.in","r",stdin);
    freopen("bus.out","w",stdout);
    cin>>n>>m>>k;
    for(int i=1;i<=m;i++){
        cin>>u[i]>>v[i]>>a[i];
    }
    cout<<"-1"<<endl;
    return 0;
}
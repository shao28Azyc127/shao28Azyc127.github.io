#include <bits/stdc++.h>
using namespace std;

int main()
{
	freopen("bus.in", "r", stdin);
	freopem("bus.out", "w", stdout);
	int n, m, q;
	cin >> n >> m >> q;
	for(int i = 1; i <= n; i++)
	{
		int u, v, w;
		cin >> u >> v >> w;
	}
	cout << -1;
	return 0;
}

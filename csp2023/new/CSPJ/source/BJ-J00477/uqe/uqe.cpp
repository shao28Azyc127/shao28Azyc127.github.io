#include <iostream>
#include <cstdio>
#define ll long long
using namespace std;

int main()
{
    freopen("uqe.in", "r", stdin);
    freopen("uqe.out", "w", stdout);

    ll t, m, a, b, c;
    cin >> t >> m;
    while (t--)
    {
        cin >> a >> b >> c;
        cout << "NO" << endl;
    }

    fclose(stdin);
    fclose(stdout);
    return 0;
}

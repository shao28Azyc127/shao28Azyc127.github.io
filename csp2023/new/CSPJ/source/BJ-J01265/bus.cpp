#include <bits/stdc++.h>
using namespace std;
const int N = 1e6+5;
int n, m, k;
int a[N], b[N], c[N];
int main() {
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    cin >> n >> m >> k;
    for (int i = 1; i <= m; i++) {
        cin >> a[i] >> b[i] >> c[i];
    }
    cout << -1;
    return 0;
}
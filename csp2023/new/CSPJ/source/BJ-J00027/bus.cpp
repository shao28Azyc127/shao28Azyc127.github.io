#include <iostream>
#include <stdio.h>
#include <math.h>
#include <algorithm>
using namespace std;


int main(){
    freopen("bus.in", "r", stdin);
    freopen("bus.out", "w", stdout);
    
    int n, m, k;
    int u[2010], v[2010], a[2010];
    
    for (int i = 1; i <= m; i++){
        cin >> u[i] >> v[i] >> a[i];
    }
    
    cout << "-1";
    return 0;
}

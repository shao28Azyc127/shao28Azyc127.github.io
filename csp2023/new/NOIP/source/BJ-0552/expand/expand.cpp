#include <bits/stdc++.h>
using namespace std;

int main(){
    int c,n,m,q;
    freopen("expand.in", "r", stdin);
    freopen("expand.out", "w", stdout);
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    cin >> c >> n >> m >> q;
    for (int i=0;i<=q;i++){
        cout << 0;
    }
    cout << endl;
    return 0;
    // bu ke yi zong si ling
}
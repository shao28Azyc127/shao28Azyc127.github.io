#include <bits/stdc++.h>
using namespace std;
int main() {
    freopen("expand.in", "r", stdin);
    freopen("expand.out", "w", stdout);
    // 不可以，总司令
    int n, m, c, q;
    scanf("%d%d%d%d", &n, &m, &c, &q);
    q++;
    while (q--) {
		putchar('0');
	}
    return 0;
}

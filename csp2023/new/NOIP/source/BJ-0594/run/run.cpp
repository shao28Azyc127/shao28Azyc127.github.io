#include <bits/stdc++.h>

using namespace std;
const int N = 100010
int main()
{
    freopen("run.in","r",stdin);
    freopen("run.out","w",stdout);
    int n,m,k,d;
    int a[N],b[N],c[N];
    for(int i = 1;i <= m;i++){
        cin >> a[i] << b[i] << c[i];
    }
    //time is up but it does not matter ^-^
    //I love Wan er forever !
    return 0;
}

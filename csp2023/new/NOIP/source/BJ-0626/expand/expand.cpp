#include<bits/stdc++.h>
#define maxn 5005
using namespace std;
string s[maxn];
long long n,a[maxn],b[maxn],A[maxn],B[maxn],m,c,q,kx,ky,px,py,vx,vy;


int main(){
    freopen("expand.in","r",stdin);
    freopen("expand.out","w",stdout);
    cin>>c>>n>>m>>q;
    for(int i=1;i<=n;i++){
        cin>>a[i];
    }
    for(int j=1;j<=m;j++){
        cin>>b[j];
    }
    while(q--){
            cin>>px>>vx>>py>>vy;
        for(int i=1;i<=n;i++){
        A[i]=a[i];
    }
    for(int j=1;j<=m;j++){
        B[j]=b[j];
    }
    cout<<"1";
    }
    cout<<"1";
return 0;
}

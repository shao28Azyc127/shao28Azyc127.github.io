#include <fstream>

using namespace std;

ifstream cin("expand.in");
ofstream cout("expand.out");

int main()
{
	int a, b, c, q;
	cin >> a >> b >> c >> q;
	for (int i = 1; i <= q; i++)
		cout << 0;
	return 0;
}
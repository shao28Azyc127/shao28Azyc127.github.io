#include <iostream>
using namespace std;
int t;
int main()
{
    freopen("run.in", "r", stdin);
    freopen("run.out", "w", stdout);
    cin >> t >> t;
    while (t--)
        cout << 0 << endl;
    return 0;
}

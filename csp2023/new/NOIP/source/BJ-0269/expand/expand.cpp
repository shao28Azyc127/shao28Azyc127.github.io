#include <iostream>
#include <cstdio>
using namespace std;
int main()
{
	freopen("expand.in" , "r" , stdin);
	freopen("expand.out" , "w" , stdout);
	int q;
	cin >> q >> q >> q>> q;
	for(int i = 1 ; i <= 1 + q ; i ++)
	{
		cout << 0;
	}
	return 0;
}